{
  "name": "ee4802-g20-tool",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "cf-typegen": "wrangler types",
    "typecheck": "tsc --noEmit",
    "check": "npm run cf-typegen && npm run typecheck && wrangler deploy --dry-run",
    "test": "vitest",
    "test:watch": "vitest",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev --remote",
    "start": "wrangler dev --remote"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.7.6",
    "hono": "^4.12.14",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "vitest": "^3.2.4",
    "wrangler": "^4.83.0"
  }
}
